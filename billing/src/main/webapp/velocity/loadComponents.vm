<html>
    <head>
        <title>Compoonentes de la familia ${familyName}</title>
    </head>
    <body>
        <p>Lista de los componentes de la familia $!{familyName}</p>
        <form action="#springUrl('/addComponents.htm')" method="post">
            #foreach ( $component in $components )
                <input type="checkbox" name="$component.code" value="$component.code" >${component.description}</input>
            #end
            <input type="submit" value="Añadir"/>
        </form>
    </body>
</html>